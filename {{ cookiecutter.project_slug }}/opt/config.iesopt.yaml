{% set active_addons = cookiecutter.add_addons.split(',') | map('trim') | list %}
{% set active_templates = cookiecutter.add_templates.split(',') | map('trim') | list %}

# ::: -------------------------------------------------------------------- ::: #
# ::: >>> SECTION | MAIN CONFIGURATION <<<                                 ::: #
# ::: -------------------------------------------------------------------- ::: #
parameters:
  - default

config:
  general:
    name:
      model: {{ cookiecutter.project_slug }}
      scenario: Basic
  # -------------------------------------------------------------------------- #
  optimization:
    problem_type: LP
    snapshots:
      count: 24
    solver:
      name: highs
  # -------------------------------------------------------------------------- #
  results:
    enabled: true
  # -------------------------------------------------------------------------- #
  files:
    data: example.csv
  # -------------------------------------------------------------------------- #
  paths:
    files: data/timeseries/
    components: data/components/
    parameters: data/parameters/
    templates: templates/
    results: out/

# ::: -------------------------------------------------------------------- ::: #
# ::: >>> SECTION | ENERGY CARRIERS <<<                                    ::: #
# ::: -------------------------------------------------------------------- ::: #
carriers:
  electricity: {}

{% if cookiecutter.add_addons != 'none' %}
addons:
  {% if 'heat' in active_addons %}
  MyHeatAddon: {}
  MyHeatAddon2: {}
  {% endif %}
{% endif %}
# ::: -------------------------------------------------------------------- ::: #
# ::: >>> SECTION | MODEL COMPONENTS <<<                                   ::: #
# ::: -------------------------------------------------------------------- ::: #
# Uncomment the following lines to load components from CSV files:
# load_components:
#   - some_specific_model_file.csv

components:
  grid:
    type: Node
    carrier: electricity
  
  market:
    type: Profile
    carrier: electricity
    node_to: grid
    mode: create
    cost: 100.0
  
  demand:
    type: Profile
    carrier: electricity
    node_from: grid
    value: 07_demand1_value@data
